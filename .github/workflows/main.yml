on: [push]

jobs:
  build_app:
    steps:
      - uses: actions/checkout@v2
      - name: build application
        id: compile
        uses: aurumcodex/jdkenv@main
        run: CGO_ENABLED=0 go build; ./jdkenv help